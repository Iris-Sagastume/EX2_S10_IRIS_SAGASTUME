name: ex2_s10_iris_sgastume
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.18.0
  firebase_auth: ^4.5.0  
  cloud_firestore: ^4.5.0
  provider: ^6.0.1
  image_picker: ^0.8.4+4
  go_router: ^6.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  


flutter:
  uses-material-design: true
